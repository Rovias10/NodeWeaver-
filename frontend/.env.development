# Configuración para `npm run dev` en local con XAMPP.
# Cambia esta URL si tu Apache sirve el proyecto bajo otra ruta.
# El backend acepta la ruta lógica como query string (?route=...).
VITE_API_BASE=http://localhost/NodeWeaver-/API/index.php
